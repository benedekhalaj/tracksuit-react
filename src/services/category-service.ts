const getCategories = (): string[] => {
    return ["Terraforming Rate", "Milestone Points", "Award Points", "Greeneries",
        "Greeneries around Cities", "Victory Points on Cards", "Money"];
}

const methods = {
    getCategories: getCategories
};

export default methods;
